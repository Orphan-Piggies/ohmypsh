.TH PSH 1 "2026" "psh" "User Commands"
.SH NAME
psh \- the pistachio shell
.SH SYNOPSIS
.B psh
.br
.B psh
.I script
.RI [ args ...]
.br
.B psh \-c
.I command
.SH DESCRIPTION
.B psh
is a Unix shell with a familiar sh-like surface and the classic traps
removed. Its two founding rules:
.IP \(bu 2
A variable expansion
.B never
word-splits:
.B rm $FILE
removes exactly one file, quoted or not.
.IP \(bu 2
Command substitution
.B $(...)
splits on
.B newlines only.
.B for f in $(ls)
iterates files; spaces in names survive.
.PP
Consequently
.B IFS
is ignored
.B everywhere
\- it neither enables splitting in expansion nor tunes
.BR read ,
which splits on blanks (hardcoded) only when given several names.
Setting IFS changes nothing; there is no partially-honored mode to
be surprised by. When splitting is genuinely wanted, route it
through the one gate that splits \- command substitution:
.B ls $(echo $args | tr \(dq \(dq \(dq\en\(dq)
.SH OPTIONS
.TP
.BI \-c " command"
Run
.I command
and exit.
.SH LANGUAGE
Pipelines
.RB ( a\ |\ b ),
redirects with optional fd numbers
.RB ( >\ >>\ <\ <>\ 2>\ 2>&1\ 2>&\- ),
heredocs
.RB ( <<EOF ,\ <<\-\ tab-stripped,\ quoted\ delimiter\ =\ verbatim),
sockets as redirect targets
.RB ( exec\ 3<>/dev/tcp/HOST/PORT ,
also
.BR /dev/udp ),
lists
.RB ( &&\ ||\ ; ),
background jobs
.RB ( & ),
.BR if / elif / else ,
.BR while ,
.BR for ,
.BR case ... esac
with glob patterns, functions
.RB ( name()\ {\ ...;\ } )
with
.BR $1 ...\c
.BR $9 ,
.BR $# ,
and a true
.B $@
splat, arithmetic
.BR $((...)) ,
parameter operators
.RB ( ${v#pat}\ ${v%pat}\ ${v/old/new}\ ${#v} ,
with literal-string replace \- documented psh flavor),
.B ~
expansion, globbing, and
.B #
comments. Interactive input continues across lines when a construct
is open.
.SH BUILTINS
.BR cd ", " exec ", " exit ", " pwd ", " jobs ", " fg ", " bg ", " wait ", "
.BR test " / " [ ", " type ", " command\ \-v ", " set\ \-e\ \-u\ /\ \-o\ pipefail ", "
.BR trap ", " export ", " local ", " readonly ", " unset ", " source " / " . ", "
.BR read ", " return ", " break ", " continue ", " history ", " help .
Run
.B help
inside psh for one-line summaries. There may be more.
.SH JOB CONTROL
.B cmd &
runs in the background; Ctrl\-Z stops the foreground job;
.BR jobs ,
.BR fg\ [%n] ,
.B bg
manage them. Each pipeline is one process group; the terminal belongs
to the foreground job.
.SH FILES
.TP
.I ~/.pshrc
Sourced at interactive startup.
.TP
.I ~/.psh_history
Command history, persisted across sessions.
.SH OH-MY-PSH
The bundled framework (see
.I omp/
in the source tree): themes are
.B PSH_PROMPT
templates re-expanded before every prompt; plugins register functions
on precmd/chpwd hooks. The
.B python
plugin auto-activates a project's
.I .venv
when you cd in. Drive it with
.BR omp\ list ,
.BR omp\ enable ,
.BR omp\ theme .
.SH EXIT STATUS
That of the last command; 127 command not found, 126 not executable,
128+N killed by signal N, 2 syntax error.
.SH SEE ALSO
.BR bash (1),
.BR fish (1),
.BR zsh (1)
\(em respected elders.
.SH HISTORY
Started as a pun. The pistachio has one of the hardest shells in the
world; 2000 fuzzed inputs and an AddressSanitizer suite agree this
one keeps the tradition. Unicode offers no pistachio emoji, an
injustice this project declines to accept.
